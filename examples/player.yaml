# Player class, YAML spec for lua-to-cpp
class:
  cpp_name: "Player"
  properties:
    - name: "maxHealth"
      type: number
      default: 0
    - name: "playerName"
      type: string
      default: "player"
    - name: "hasAmulet"
      type: boolean
      default: false
